import 'package:flutter/material.dart';

class OfferLift extends StatelessWidget {
  const OfferLift({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("jhig"),
    );
  }
}
